function sum(a: number, b: number) {
  return a + b;
}

sum(1, 1.1);
enum rate {
  rate1 = 1.2,
  rate2 = 2,
}
let increase: rate = rate.rate1;
function calculateTax(income: number): number {
  if (income < 50_000) return income * rate.rate1;
  return income * rate.rate2;

  //   if (income < 50_000) {
  //     return income * 1.2;
  //   } else {
  //     return income * 2;
  //   }
}
console.log(calculateTax(50_001));
